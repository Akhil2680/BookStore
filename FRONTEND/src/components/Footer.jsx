import React from "react";

const Footer = () => {
  return (
    <footer className="footer mt-auto">
      <p>&copy; {new Date().getFullYear()} Book Store. Built with ❤️ using React.</p>
    </footer>
  );
};

export default Footer;
